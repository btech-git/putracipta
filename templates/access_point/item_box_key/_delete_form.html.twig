<form method="post" action="{{ path('app_access_point_item_box_key_delete', {'id': item_box_key.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ item_box_key.id) }}">
    <button class="btn">Delete</button>
</form>
