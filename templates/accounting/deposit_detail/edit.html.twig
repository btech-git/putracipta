{% extends 'layout_page.html.twig' %}

{% block title 'Edit Deposit Detail' %}

{% block toolbar %}
    <a class="btn btn-primary btn-sm" href="{{ path('app_accounting_deposit_detail_index') }}">Manage</a>
    <a class="btn btn-info btn-sm" href="{{ path('app_accounting_deposit_detail_show', {'id': depositDetail.id}) }}">Show</a>
{% endblock %}

{% block content %}
    {{ include('accounting/deposit_detail/_form.html.twig') }}
{% endblock %}
