{% extends 'layout_page.html.twig' %}

{% block title 'Edit Expense Detail' %}

{% block toolbar %}
    <a class="btn btn-primary btn-sm" href="{{ path('app_accounting_expense_detail_index') }}">Manage</a>
    <a class="btn btn-info btn-sm" href="{{ path('app_accounting_expense_detail_show', {'id': expenseDetail.id}) }}">Show</a>
{% endblock %}

{% block content %}
    {{ include('accounting/expense_detail/_form.html.twig') }}
{% endblock %}
