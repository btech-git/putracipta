<div>
    <h6>New Transactions</h6>
    <table class="table table-bordered">
        <thead class="table-primary">
            <tr>
                <th>Draft Request Material</th>
                <th>Draft Request Kertas</th>
                <th>Approved Request Material</th>
                <th>Approved Request Kertas</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td class="text-center font-weight-bold"><a href="{{ path('app_purchase_purchase_request_header_head') }}">{{ purchaseRequestHeaderCount }}</a></td>
                <td class="text-center font-weight-bold"><a href="{{ path('app_purchase_purchase_request_paper_header_head') }}">{{ purchaseRequestPaperHeaderCount }}</a></td>
                <td class="text-center font-weight-bold"><a href="{{ path('app_purchase_purchase_request_header_approval') }}">{{ purchaseRequestHeaderCountApproval }}</a></td>
                <td class="text-center font-weight-bold"><a href="{{ path('app_purchase_purchase_request_paper_header_approval') }}">{{ purchaseRequestPaperHeaderCountApproval }}</a></td>
            </tr>
        </tbody>
    </table>
    <table class="table table-bordered">
        <thead class="table-primary">
            <tr>
                <th>PO Material</th>
                <th>PO Kertas</th>
                <th>Invoice Pembelian</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td class="text-center font-weight-bold"><a href="{{ path('app_purchase_purchase_order_header_head') }}">{{ purchaseOrderHeaderCount }}</a></td>
                <td class="text-center font-weight-bold"><a href="{{ path('app_purchase_purchase_order_paper_header_head') }}">{{ purchaseOrderPaperHeaderCount }}</a></td>
                <td class="text-center font-weight-bold"><a href="{{ path('app_purchase_purchase_invoice_header_head') }}">{{ purchaseInvoiceHeaderCount }}</a></td>
            </tr>
        </tbody>
    </table>
    <table class="table table-bordered">
        <thead class="table-primary">
            <tr>
                <th>Order Penjualan</th>
                <th>Invoice Penjualan</th>
                <th>Permintaan Produksi</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td class="text-center font-weight-bold"><a href="{{ path('app_sale_sale_order_header_head') }}">{{ saleOrderHeaderCount }}</a></td>
                <td class="text-center font-weight-bold"><a href="{{ path('app_sale_sale_invoice_header_head') }}">{{ saleInvoiceHeaderCount }}</a></td>
                <td class="text-center font-weight-bold"><a href="{{ path('app_stock_inventory_request_header_head') }}">{{ inventoryRequestHeaderCount }}</a></td>
            </tr>
        </tbody>
    </table>
</div>
                
{% import 'app/data_grid.html.twig' as grid %}

{{ grid.form(form) }}

{{ grid.info(form, count, saleOrderHeaders) }}

<div class="table-responsive">
    <table class="table table-bordered table-striped">
        <thead>
            <tr class="table-primary">
                <th style="min-width: 280px">
                    SO #
                </th>
                <th style="min-width: 150px">
                    MO #
                </th>
                <th style="min-width: 350px">
                    Delivery #
                </th>
                <th style="min-width: 200px">
                    Invoice #
                </th>
                <th style="min-width: 200px">
                    Payment #
                </th>
            </tr>
        </thead>
        <tbody>
            {% for saleOrderHeader in saleOrderHeaders %}
                {% set currentSaleOrderHeader = saleOrderHeaderData[saleOrderHeader.id]|default(saleOrderHeader) %}
                {% set saleOrderDetails = currentSaleOrderHeader.saleOrderDetails %}
                {% set masterOrderProductDetails = saleOrderDetails|map(saleOrderDetail => saleOrderDetail.masterOrderProductDetails)|reduce((a, b) => a|merge(b), []) %}
                {% set deliveryDetails = masterOrderProductDetails|map(masterOrderProductDetail => masterOrderProductDetail.deliveryDetails)|reduce((a, b) => a|merge(b), []) %}
                {% set saleInvoiceDetails = deliveryDetails|map(deliveryDetail => deliveryDetail.saleInvoiceDetails)|reduce((a, b) => a|merge(b), []) %}
                {% set salePaymentDetails = saleInvoiceDetails|map(saleInvoiceDetail => saleInvoiceDetail.saleInvoiceHeader.salePaymentDetails)|reduce((a, b) => a|merge(b), []) %}
                <tr>
                    <td>{{ currentSaleOrderHeader.codeNumber }}</td>
                    <td>{{ masterOrderProductDetails|map(masterOrderProductDetail => masterOrderProductDetail.masterOrderHeader.codeNumber)|join(', ') }}</td>
                    <td>{{ deliveryDetails|map(deliveryDetail => deliveryDetail.deliveryHeader.codeNumber)|join(', ') }}</td>
                    <td>{{ saleInvoiceDetails|map(saleInvoiceDetail => saleInvoiceDetail.saleInvoiceHeader.codeNumber)|join(', ') }}</td>
                    <td>{{ salePaymentDetails|map(salePaymentDetail => salePaymentDetail.salePaymentHeader.codeNumber)|join(', ') }}</td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="5">No records found</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</div>

{{ grid.navigation(form, count, saleOrderHeaders) }}

            