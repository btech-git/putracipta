<form method="post" action="{{ path('app_master_account_category_delete', {'id': account_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ account_category.id) }}">
    <button class="btn">Delete</button>
</form>
