{% extends 'layout_page.html.twig' %}

{% block title 'Show Brand' %}

{% block toolbar %}
    <a class="btn btn-primary btn-sm" href="{{ path('app_master_brand_index') }}">Manage</a>
    <a class="btn btn-warning btn-sm" href="{{ path('app_master_brand_edit', {'id': brand.id}) }}">Edit</a>
{% endblock %}

{% block content %}
    <table class="table table-bordered table-striped">
        <tbody>
            <tr>
                <th>Is Inactive</th>
                <td>{{ brand.isInactive ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ brand.name }}</td>
            </tr>
        </tbody>
    </table>

    {{ include('master/brand/_delete_form.html.twig') }}
{% endblock %}
