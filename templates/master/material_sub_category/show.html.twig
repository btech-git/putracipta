{% extends 'layout_page.html.twig' %}

{% block title 'Show Material Sub Category' %}

{% block toolbar %}
    <a class="btn btn-primary btn-sm" href="{{ path('app_master_material_sub_category_index') }}">Manage</a>
    <a class="btn btn-warning btn-sm" href="{{ path('app_master_material_sub_category_edit', {'id': materialSubCategory.id}) }}">Edit</a>
{% endblock %}

{% block content %}
    <table class="table table-bordered table-striped">
        <tbody>
            <tr>
                <th>Code</th>
                <td>{{ materialSubCategory.code }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ materialSubCategory.name }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ materialSubCategory.materialCategory.name }}</td>
            </tr>
        </tbody>
    </table>

    {#{{ include('master/material_sub_category/_delete_form.html.twig') }}#}
{% endblock %}
