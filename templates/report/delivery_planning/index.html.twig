{% extends 'layout_page.html.twig' %}

{% block title 'Rencana Kirim PO Customer' %}

{% block content %}
    {% for type, flashes in app.session.flashBag.all %}
        {% for flash in flashes %}
            <div class="alert alert-{{ type }} alert-dismissible fade show" role="alert">
                <strong>{{ flash.title }}</strong> {{ flash.message }}
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        {% endfor %}
    {% endfor %}

    <div data-controller="html-loader"
         data-html-loader-url-value="{{ path('app_report_delivery_planning__list') }}"
         data-html-loader-auto-load-value="true">
    </div>
{% endblock %}
