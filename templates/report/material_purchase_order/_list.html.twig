{% import 'app/data_grid.html.twig' as grid %}

<div class="d-print-none">
    {{ grid.form(form) }}

    <div class="my-3 px-3 bg-light" {{ grid.filter_link(form) }}>
        <div class="row">
            <div class="col">
                <div class="my-2 row">
                    <label class="col-sm-2 col-form-label w-25">Tanggal</label>
                    <div class="col-sm-10 w-75">
                        <div class="row">
                            <div class="col">
                                <input class="form-control readonly-form-input" type="text" data-controller="flatpickr-element" {{ grid.filter_link_item(form, 'purchaseOrderHeader:transactionDate', [form.filter['purchaseOrderHeader:transactionDate'].0.vars.choices.0.value, true, false]) }} value="{{ form.filter['purchaseOrderHeader:transactionDate'].1.vars.value }}" />
                            </div>
                            <div class="col">
                                <input class="form-control readonly-form-input" type="text" data-controller="flatpickr-element" {{ grid.filter_link_item(form, 'purchaseOrderHeader:transactionDate', [form.filter['purchaseOrderHeader:transactionDate'].0.vars.choices.0.value, false, true]) }} value="{{ form.filter['purchaseOrderHeader:transactionDate'].2.vars.value }}" />
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col">
                <div class="my-2 row">
                    <label class="col-sm-2 col-form-label w-25">Status</label>
                    <div class="col-sm-10 w-75">
                        <input class="form-control" type="text" {{ grid.filter_link_item(form, 'purchaseOrderHeader:transactionStatus') }} value="{{ form.filter['purchaseOrderHeader:transactionStatus'].1.vars.value }}" />
                    </div>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col">
                <div class="my-2 row">
                    <label class="col-sm-2 col-form-label w-25">Kode</label>
                    <div class="col-sm-10 w-75">
                        <input class="form-control" type="text" {{ grid.filter_link_item(form, 'code') }} value="{{ form.filter.code.1.vars.value }}" />
                    </div>
                </div>
            </div>
            <div class="col">
                <div class="my-2 row">
                    <label class="col-sm-2 col-form-label w-25">Material</label>
                    <div class="col-sm-10 w-75">
                        <input class="form-control" type="text" {{ grid.filter_link_item(form, 'name') }} value="{{ form.filter.name.1.vars.value }}" />
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="d-grid">
        <div class="row">
            <div class="col text-center">
                <button type="submit" class="btn btn-primary btn-sm"
                        data-controller="redirection"
                        data-redirection-url-value="{{ path('app_report_material_purchase_order__list') }}"
                        data-redirection-form-target-value="form[name={{ form.vars.name }}]"
                        data-redirection-extra-values-param="{{ {export: 'export'}|json_encode }}"
                        data-action="redirection#redirect">
                    Export
                </button>
            </div>
        </div>
    </div>

    {{ grid.info(form, count, materials) }}

    <hr />
</div>
    
<div class="report">
    <div class="report-header">
        <div class="report-title">PT. Putracipta Karindomas</div>
        <div class="report-title">Laporan PO per Material</div>
        <div>
            {% set startDate = form.vars.value.filter['purchaseOrderHeader:transactionDate'].1|default %}
            {% set endDate = form.vars.value.filter['purchaseOrderHeader:transactionDate'].2|default %}
            {{ startDate ? startDate|date('j F Y') }} &nbsp;&ndash;&nbsp; {{ endDate ? endDate|date('j F Y') }}
        </div>
    </div>

    <br />

    <table class="report-data">
        <thead class="border-top-solid-bold border-bottom-solid-bold">
            <tr>
                <th class="text-start">Kode</th>
                <th class="text-start">Nama Material</th>
                <th class="text-start">Category</th>
                <th class="text-start">Sub Category</th>
            </tr>
            <tr>
                <td colspan="4">
                    <table>
                        <thead>
                            <tr>
                                <th class="text-start">PO #</th>
                                <th class="text-start">Tanggal</th>
                                <th class="text-start">Supplier</th>
                                <th class="text-start">Quantity</th>
                                <th class="text-start">Unit Price</th>
                                <th class="text-start">Total</th>
                            </tr>
                        </thead>
                    </table>
                </td>
            </tr>
        </thead>
        <tbody class="border-top-solid-bold border-bottom-solid-bold">
            {% for material in materials %}
                <tr>
                    <td class="text-start">{{ material.codeNumber|default }}</td>
                    <td class="text-start">{{ material.name|default }}</td>
                    <td class="text-start">{{ material.materialSubCategory.materialCategory.name|default }}</td>
                    <td class="text-start">{{ material.materialSubCategory.name|default }}</td>
                </tr>
                <tr class="border-bottom-dotted-bold">
                    <td colspan="4">
                        <table>
                            <tbody>
                                {% set totalSum = '0.00' %}
                                {% for purchaseOrderDetail in purchaseOrderDetails[material.id] %}
                                    <tr>
                                        <td class="text-start">{{ purchaseOrderDetail.purchaseOrderHeader.codeNumber|default }}</td>
                                        <td class="text-start">{{ purchaseOrderDetail.purchaseOrderHeader.transactionDate|default ? purchaseOrderDetail.purchaseOrderHeader.transactionDate|date('j F Y') }}</td>
                                        <td class="text-start">{{ purchaseOrderDetail.purchaseOrderHeader.supplier.company|default }}</td>
                                        <td class="text-center">{{ purchaseOrderDetail.quantity|default|number_format(2) }}</td>
                                        <td class="text-end">{{ purchaseOrderDetail.unitPrice|default|number_format(2) }}</td>
                                        <td class="text-end">{{ purchaseOrderDetail.total|default|number_format(2) }}</td>
                                    </tr>
                                    {% set totalSum = totalSum + purchaseOrderDetail.total %}
                                {% endfor %}
                            </tbody>
                            <tfoot class="border-top-solid-normal">
                                <tr>
                                    <td class="text-end" colspan="5"><strong>Total</strong></td>
                                    <td class="text-end"><strong>{{ totalSum|number_format(2) }}</strong></td>
                                </tr>
                            </tfoot>
                        </table>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</div>

<div class="d-print-none">
    <br />
    {{ grid.navigation(form, count, materials) }}
</div>
