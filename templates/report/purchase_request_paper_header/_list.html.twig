{% import 'app/data_grid.html.twig' as grid %}

<div class="d-print-none">
    {{ grid.form(form) }}

    {{ grid.info(form, count, purchaseRequestPaperHeaders) }}

    {#<div class="text-center">
        <button type="button" class="btn btn-info btn-sm" id="export">Export</button>
    </div>#}

    <hr />
</div>

<div class="report">
    <div class="report-header">
        <div class="report-title">PT. Putracipta Karindomas</div>
        <div class="report-title">Laporan Purchase Request Kertas</div>
        {#<div>
            {% set startDate = grid.searchVals['purchaseRequestPaperHeader']['transactionDate'][1]|default %}
            {% set endDate = grid.searchVals['purchaseRequestPaperHeader']['transactionDate'][2]|default %}
            {{ startDate ? startDate|date('j F Y') }} &nbsp;&ndash;&nbsp; {{ endDate ? endDate|date('j F Y') }}
        </div>#}
    </div>

    <br />

    <table class="report-data">
        <thead class="border-top-solid-bold border-bottom-solid-bold">
            <tr>
                <th class="text-start">Request #</th>
                <th class="text-start">Tanggal</th>
                <th class="text-start">Gudang</th>
                <th class="text-start">Status</th>
                <th class="text-start">Note</th>
            </tr>
            <tr>
                <td colspan="5">
                    <table>
                        <thead>
                            <tr>
                                <th class="text-start">Kode</th>
                                <th class="text-start">Nama Produk</th>
                                <th class="text-start">Qty</th>
                                <th class="text-start">Satuan</th>
                                <th class="text-start">Tanggal Pakai</th>
                                <th class="text-start">Memo</th>
                                <th class="text-start">Status</th>
                            </tr>
                        </thead>
                    </table>
                </td>
            </tr>
        </thead>
        <tbody class="border-top-solid-bold border-bottom-solid-bold">
            {% for purchaseRequestPaperHeader in purchaseRequestPaperHeaders %}
                <tr>
                    <td class="text-start">{{ purchaseRequestPaperHeader.codeNumber|default }}</td>
                    <td class="text-start">{{ purchaseRequestPaperHeader.transactionDate|default ? purchaseRequestPaperHeader.transactionDate|date('j M Y') }}</td>
                    <td class="text-start">{{ purchaseRequestPaperHeader.warehouse.name|default }}</td>
                    <td class="text-start">{{ purchaseRequestPaperHeader.transactionStatus|default }}</td>
                    <td class="text-start">{{ purchaseRequestPaperHeader.note|default }}</td>
                </tr>
                <tr class="border-bottom-dotted-bold">
                    <td colspan="5">
                        <table>
                            <tbody>
                                {% for purchaseRequestPaperDetail in purchaseRequestPaperHeader.purchaseRequestPaperDetails %}
                                    <tr>
                                        <td class="text-start">{{ purchaseRequestPaperDetail.paper.codeNumber|default }}</td>
                                        <td class="text-start">{{ purchaseRequestPaperDetail.paper.paperNameSizeCombination|default }}</td>
                                        <td class="text-start">{{ purchaseRequestPaperDetail.quantity|default|number_format(0) }}</td>
                                        <td class="text-start">{{ purchaseRequestPaperDetail.unit.name|default }}</td>
                                        <td class="text-start">{{ purchaseRequestPaperDetail.usageDate|default ? purchaseRequestPaperDetail.usageDate|date('j M Y') }}</td>
                                        <td class="text-end">{{ purchaseRequestPaperDetail.memo|default }}</td>
                                        <td class="text-end">{{ purchaseRequestPaperDetail.transactionStatus|default }}</td>
                                    </tr>
                                {% endfor %}
                            </tbody>
                            <tfoot class="border-top-solid-normal">
                                <tr>
                                    <td class="text-end" colspan="2"><strong>Total</strong></td>
                                    <td class="text-end"><strong>{{ purchaseRequestPaperHeader.totalQuantity|default|number_format(0) }}</strong></td>
                                    <td colspan="4">&nbsp;</td>
                                </tr>
                            </tfoot>
                        </table>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</div>

<div class="d-print-none">
    <br />
    {{ grid.navigation(form, count, purchaseRequestPaperHeaders) }}
</div>
