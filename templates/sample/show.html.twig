{% extends 'base.html.twig' %}

{% block title %}Sample{% endblock %}

{% block body %}
    <h1>Sample</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sample.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ sample.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ sample.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_sample_index') }}">back to list</a>

    <a href="{{ path('app_sample_edit', {'id': sample.id}) }}">edit</a>

    {{ include('sample/_delete_form.html.twig') }}
{% endblock %}
