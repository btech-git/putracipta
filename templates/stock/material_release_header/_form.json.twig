{
    "totalQuantity": "{{ materialRequestHeader.totalQuantity|default|number_format(0) }}"
    "materialReleaseDetails": {
        {%- for materialReleaseDetailForm in form.materialReleaseDetails -%}
            {%- set materialReleaseDetail = materialReleaseDetailForm.vars.data -%}
            "{{ materialReleaseDetailForm.vars.name }}": {
            
            }
            {%- if not loop.last -%},{%- endif -%}
        {%- endfor -%}
    }
}
