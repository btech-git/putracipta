<form method="post" action="{{ path('app_transaction_purchase_order_detail_delete', {'id': purchaseOrderDetail.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ purchaseOrderDetail.id) }}">
    <div class="d-grid">
        <button class="btn btn-danger btn-sm"
                data-controller="dom-element"
                data-action="dom-element#confirm"
                data-dom-element-confirm-message-param="Are you sure you want to delete this item?">
            Delete
        </button>
    </div>
</form>
