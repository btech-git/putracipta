{% extends 'layout_page.html.twig' %}

{% block title 'Show Purchase Order Detail' %}

{% block toolbar %}
    <a class="btn btn-primary btn-sm" href="{{ path('app_transaction_purchase_order_detail_index') }}">Manage</a>
    <a class="btn btn-warning btn-sm" href="{{ path('app_transaction_purchase_order_detail_edit', {'id': purchaseOrderDetail.id}) }}">Edit</a>
{% endblock %}

{% block content %}
    <table class="table table-bordered table-striped">
        <tbody>
            <tr>
                <th>Quantity</th>
                <td>{{ purchaseOrderDetail.quantity }}</td>
            </tr>
            <tr>
                <th>Unit Price</th>
                <td>{{ purchaseOrderDetail.unitPrice }}</td>
            </tr>
            <tr>
                <th>Discount</th>
                <td>{{ purchaseOrderDetail.discount }}</td>
            </tr>
            <tr>
                <th>Is Canceled</th>
                <td>{{ purchaseOrderDetail.isCanceled ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    {{ include('transaction/purchase_order_detail/_delete_form.html.twig') }}
{% endblock %}
