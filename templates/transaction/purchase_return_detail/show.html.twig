{% extends 'layout_page.html.twig' %}

{% block title 'Show Purchase Return Detail' %}

{% block toolbar %}
    <a class="btn btn-primary btn-sm" href="{{ path('app_transaction_purchase_return_detail_index') }}">Manage</a>
    <a class="btn btn-warning btn-sm" href="{{ path('app_transaction_purchase_return_detail_edit', {'id': purchaseReturnDetail.id}) }}">Edit</a>
{% endblock %}

{% block content %}
    <table class="table table-bordered table-striped">
        <tbody>
            <tr>
                <th>Quantity</th>
                <td>{{ purchaseReturnDetail.quantity }}</td>
            </tr>
            <tr>
                <th>Unit Price</th>
                <td>{{ purchaseReturnDetail.unitPrice }}</td>
            </tr>
            <tr>
                <th>Is Canceled</th>
                <td>{{ purchaseReturnDetail.isCanceled ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    {{ include('transaction/purchase_return_detail/_delete_form.html.twig') }}
{% endblock %}
